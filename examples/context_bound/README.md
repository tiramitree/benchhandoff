# Context-bound example

This version 2 suite binds the bytes of `context.json`, the resolved executable
content and path identity, a non-inheriting launch environment, and the
platform process-scope policy. The descriptor is a synthetic public fixture,
not a VM image, container snapshot, remote attestation, or production record.

Run it from a copy of this directory so `output.txt` starts absent.
