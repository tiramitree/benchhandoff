apiVersion: control.benchhandoff.dev/v1alpha1
kind: AgentRun
metadata:
  name: wrong-approval
  namespace: benchhandoff-e2e
  labels:
    test.benchhandoff.dev/case: wrong-approval
spec:
  execution:
    pvcName: benchhandoff-e2e-data
    suitePath: wrong-approval/suite.toml
    suiteSHA256: SUITE_SHA256
    runnerImage: RUNNER_IMAGE
    activeDeadlineSeconds: 600
